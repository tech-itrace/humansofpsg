/**
 * Auto-generated alt text, replacing the old manual "Photo description"
 * CMS field (removed per client feedback — one less field for contributors
 * to fill in). Simple and accurate rather than descriptive, but keeps every
 * photo accessible without relying on contributors to write it.
 */
export function defaultPhotoAlt(name: string): string {
  return `Photo of ${name}`;
}
