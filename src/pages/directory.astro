---
/* =========================================================
   PSG PEOPLE DIRECTORY
   Radio Garden inspired geographic exploration
========================================================= */

const people = [
  {
    name: "Arun Kumar",
    country: "India",
    state: "Tamil Nadu",
    city: "Chennai",
    district: "Chennai",
    department: "Engineering",
    expertise: "Software Development",
    category: "Technology",
    branch: "Chennai",
    address: "Chennai, Tamil Nadu",
    pullQuote: "Building useful technology for people.",
    tags: ["Technology", "Software"],
    image: "/images/arun.jpg",
    lat: 13.0827,
    lng: 80.2707
  },
  {
    name: "Priya Raj",
    country: "India",
    state: "Tamil Nadu",
    city: "Coimbatore",
    district: "Coimbatore",
    department: "Management",
    expertise: "Business Management",
    category: "Business",
    branch: "Coimbatore",
    address: "Coimbatore, Tamil Nadu",
    pullQuote: "People make every journey meaningful.",
    tags: ["Business", "Management"],
    image: "/images/priya.jpg",
    lat: 11.0168,
    lng: 76.9558
  },
  {
    name: "Karthik S",
    country: "India",
    state: "Tamil Nadu",
    city: "Madurai",
    district: "Madurai",
    department: "Commerce",
    expertise: "Finance",
    category: "Finance",
    branch: "Madurai",
    address: "Madurai, Tamil Nadu",
    pullQuote: "Every number tells a story.",
    tags: ["Finance", "Commerce"],
    image: "/images/karthik.jpg",
    lat: 9.9252,
    lng: 78.1198
  },
  {
    name: "Meena Devi",
    country: "India",
    state: "Tamil Nadu",
    city: "Salem",
    district: "Salem",
    department: "Arts",
    expertise: "Content Creation",
    category: "Creative",
    branch: "Salem",
    address: "Salem, Tamil Nadu",
    pullQuote: "Creativity connects people.",
    tags: ["Creative", "Content"],
    image: "/images/meena.jpg",
    lat: 11.6643,
    lng: 78.146
  },
  {
    name: "Vijay Anand",
    country: "India",
    state: "Tamil Nadu",
    city: "Tiruchirappalli",
    district: "Tiruchirappalli",
    department: "Engineering",
    expertise: "Mechanical Engineering",
    category: "Engineering",
    branch: "Trichy",
    address: "Tiruchirappalli, Tamil Nadu",
    pullQuote: "Ideas become real when we build them.",
    tags: ["Engineering"],
    image: "/images/vijay.jpg",
    lat: 10.7905,
    lng: 78.7047
  },
  {
    name: "Divya R",
    country: "India",
    state: "Tamil Nadu",
    city: "Tirunelveli",
    district: "Tirunelveli",
    department: "Science",
    expertise: "Research",
    category: "Research",
    branch: "Tirunelveli",
    address: "Tirunelveli, Tamil Nadu",
    pullQuote: "Curiosity is the beginning of discovery.",
    tags: ["Science", "Research"],
    image: "/images/divya.jpg",
    lat: 8.7139,
    lng: 77.7567
  },
  {
    name: "Suresh Kumar",
    country: "India",
    state: "Tamil Nadu",
    city: "Thoothukudi",
    district: "Thoothukudi",
    department: "Commerce",
    expertise: "Entrepreneurship",
    category: "Business",
    branch: "Thoothukudi",
    address: "Thoothukudi, Tamil Nadu",
    pullQuote: "Small beginnings can create big journeys.",
    tags: ["Business"],
    image: "/images/suresh.jpg",
    lat: 8.7642,
    lng: 78.1348
  },
  {
    name: "Anitha M",
    country: "India",
    state: "Tamil Nadu",
    city: "Erode",
    district: "Erode",
    department: "Management",
    expertise: "Human Resources",
    category: "People",
    branch: "Erode",
    address: "Erode, Tamil Nadu",
    pullQuote: "Every person has a story worth hearing.",
    tags: ["People", "HR"],
    image: "/images/anitha.jpg",
    lat: 11.341,
    lng: 77.7172
  },
  {
    name: "Rahul P",
    country: "India",
    state: "Tamil Nadu",
    city: "Vellore",
    district: "Vellore",
    department: "Computer Science",
    expertise: "Artificial Intelligence",
    category: "Technology",
    branch: "Vellore",
    address: "Vellore, Tamil Nadu",
    pullQuote: "Technology should solve real problems.",
    tags: ["AI", "Technology"],
    image: "/images/rahul.jpg",
    lat: 12.9165,
    lng: 79.1325
  },
  {
    name: "Nandhini K",
    country: "India",
    state: "Tamil Nadu",
    city: "Kanchipuram",
    district: "Kanchipuram",
    department: "Design",
    expertise: "UI/UX Design",
    category: "Creative",
    branch: "Kanchipuram",
    address: "Kanchipuram, Tamil Nadu",
    pullQuote: "Good design makes things feel simple.",
    tags: ["Design", "Creative"],
    image: "/images/nandhini.jpg",
    lat: 12.8342,
    lng: 79.7036
  },
  {
    name: "Mohan Raj",
    country: "India",
    state: "Tamil Nadu",
    city: "Dindigul",
    district: "Dindigul",
    department: "Engineering",
    expertise: "Civil Engineering",
    category: "Engineering",
    branch: "Dindigul",
    address: "Dindigul, Tamil Nadu",
    pullQuote: "Strong foundations create lasting futures.",
    tags: ["Engineering", "Civil"],
    image: "/images/mohan.jpg",
    lat: 10.3673,
    lng: 77.9803
  },
  {
    name: "Lakshmi S",
    country: "India",
    state: "Tamil Nadu",
    city: "Thanjavur",
    district: "Thanjavur",
    department: "Arts",
    expertise: "Literature",
    category: "Creative",
    branch: "Thanjavur",
    address: "Thanjavur, Tamil Nadu",
    pullQuote: "Stories keep our memories alive.",
    tags: ["Literature", "Arts"],
    image: "/images/lakshmi.jpg",
    lat: 10.7867,
    lng: 79.1378
  },
  {
    name: "Prakash V",
    country: "India",
    state: "Tamil Nadu",
    city: "Namakkal",
    district: "Namakkal",
    department: "Commerce",
    expertise: "Marketing",
    category: "Business",
    branch: "Namakkal",
    address: "Namakkal, Tamil Nadu",
    pullQuote: "Good communication creates opportunities.",
    tags: ["Marketing"],
    image: "/images/prakash.jpg",
    lat: 11.2189,
    lng: 78.1674
  },
  {
    name: "Deepa J",
    country: "India",
    state: "Tamil Nadu",
    city: "Karur",
    district: "Karur",
    department: "Science",
    expertise: "Biotechnology",
    category: "Research",
    branch: "Karur",
    address: "Karur, Tamil Nadu",
    pullQuote: "Science begins with asking why.",
    tags: ["Science", "Research"],
    image: "/images/deepa.jpg",
    lat: 10.9601,
    lng: 78.0766
  },
  {
    name: "Saravanan P",
    country: "India",
    state: "Tamil Nadu",
    city: "Cuddalore",
    district: "Cuddalore",
    department: "Engineering",
    expertise: "Electrical Engineering",
    category: "Engineering",
    branch: "Cuddalore",
    address: "Cuddalore, Tamil Nadu",
    pullQuote: "Energy powers every possibility.",
    tags: ["Engineering", "Electrical"],
    image: "/images/saravanan.jpg",
    lat: 11.748,
    lng: 79.7714
  },
  {
    name: "Aishwarya R",
    country: "India",
    state: "Tamil Nadu",
    city: "Villupuram",
    district: "Villupuram",
    department: "Computer Science",
    expertise: "Web Development",
    category: "Technology",
    branch: "Villupuram",
    address: "Villupuram, Tamil Nadu",
    pullQuote: "The web brings people closer.",
    tags: ["Web", "Technology"],
    image: "/images/aishwarya.jpg",
    lat: 11.9401,
    lng: 79.4861
  },
  {
    name: "Manoj T",
    country: "India",
    state: "Tamil Nadu",
    city: "Tiruppur",
    district: "Tiruppur",
    department: "Management",
    expertise: "Operations",
    category: "Business",
    branch: "Tiruppur",
    address: "Tiruppur, Tamil Nadu",
    pullQuote: "Great work happens when people work together.",
    tags: ["Business", "Operations"],
    image: "/images/manoj.jpg",
    lat: 11.1085,
    lng: 77.3411
  },
  {
    name: "Revathi S",
    country: "India",
    state: "Tamil Nadu",
    city: "Kanyakumari",
    district: "Kanyakumari",
    department: "Arts",
    expertise: "Photography",
    category: "Creative",
    branch: "Kanyakumari",
    address: "Kanyakumari, Tamil Nadu",
    pullQuote: "A photograph can preserve a feeling forever.",
    tags: ["Photography", "Creative"],
    image: "/images/revathi.jpg",
    lat: 8.0883,
    lng: 77.5385
  },
  {
    name: "Dinesh K",
    country: "India",
    state: "Tamil Nadu",
    city: "Tiruvallur",
    district: "Tiruvallur",
    department: "Computer Science",
    expertise: "Cloud Computing",
    category: "Technology",
    branch: "Tiruvallur",
    address: "Tiruvallur, Tamil Nadu",
    pullQuote: "The future belongs to those who keep learning.",
    tags: ["Cloud", "Technology"],
    image: "/images/dinesh.jpg",
    lat: 13.1439,
    lng: 79.908
  },
  {
    name: "Harini M",
    country: "India",
    state: "Tamil Nadu",
    city: "Virudhunagar",
    district: "Virudhunagar",
    department: "Management",
    expertise: "Leadership",
    category: "People",
    branch: "Virudhunagar",
    address: "Virudhunagar, Tamil Nadu",
    pullQuote: "Leadership starts with listening.",
    tags: ["Leadership", "People"],
    image: "/images/harini.jpg",
    lat: 9.568,
    lng: 77.9624
  }
];

const cities = [...new Set(people.map(p => p.city))].sort();
const departments = [...new Set(people.map(p => p.department))].sort();
const expertise = [...new Set(people.map(p => p.expertise))].sort();
const categories = [...new Set(people.map(p => p.category))].sort();
---

<div class="directory-page">

  <!-- =====================================================
       HEADER
  ====================================================== -->

  <header class="directory-header">

    <div>
      <p class="eyebrow">HUMANS OF PSG</p>

      <h1>
        Global People Network
      </h1>

      <p class="subtitle">
        Explore PSGians by location.
      </p>
    </div>

    <div class="header-count">
      <strong>{people.length}</strong>
      <span>People</span>
    </div>

  </header>


  <!-- =====================================================
       MAIN
  ====================================================== -->

  <main class="directory-layout">

    <!-- =================================================
         SIDEBAR
    ================================================== -->

    <aside class="directory-sidebar">

      <div class="search-box">

        <span>⌕</span>

        <input
          id="searchInput"
          type="search"
          placeholder="Search PSGians..."
        />

      </div>


      <div class="filter-group">

        <label>City</label>

        <select id="citySelect">

          <option value="">
            All cities
          </option>

          {cities.map(city => (
            <option value={city}>
              {city}
            </option>
          ))}

        </select>

      </div>


      <div class="filter-group">

        <label>Department</label>

        <select id="departmentSelect">

          <option value="">
            All departments
          </option>

          {departments.map(department => (
            <option value={department}>
              {department}
            </option>
          ))}

        </select>

      </div>


      <div class="filter-group">

        <label>Expertise</label>

        <select id="expertiseSelect">

          <option value="">
            All expertise
          </option>

          {expertise.map(item => (
            <option value={item}>
              {item}
            </option>
          ))}

        </select>

      </div>


      <div class="category-section">

        <div class="section-title">
          Categories
        </div>

        <div
          id="categoryChips"
          class="category-chips"
        >

          <button
            class="category-chip active"
            data-category=""
            type="button"
          >
            All
          </button>

          {categories.map(category => (

            <button
              class="category-chip"
              data-category={category}
              type="button"
            >
              {category}
            </button>

          ))}

        </div>

      </div>


      <button
        id="resetButton"
        class="reset-button"
        type="button"
      >
        Reset filters
      </button>


      <div class="result-count">

        <strong id="resultCount">
          {people.length}
        </strong>

        people found

      </div>


      <div
        id="peopleList"
        class="people-list"
      ></div>

    </aside>


    <!-- =================================================
         MAP SECTION
    ================================================== -->

    <section class="map-section">

      <div class="map-topbar">

        <div>

          <p class="map-eyebrow">
            EXPLORE BY LOCATION
          </p>

          <h2 id="mapTitle">
            World
          </h2>

        </div>


        <button
          id="backButton"
          class="back-button hidden"
          type="button"
        >
          ← Back
        </button>

      </div>


      <!-- =================================================
           GLOBE
      ================================================== -->

      <div
        id="globeContainer"
        class="globe-container"
      >

        <!-- LOADING -->
        <div
          id="globeLoading"
          class="globe-loading"
        >
          <span class="loading-spinner"></span>
          <span id="loadingText">
            Loading globe...
          </span>
        </div>


        <div class="globe-crosshair">
          <span></span>
        </div>


        <div
          id="locationLabel"
          class="location-label"
        >
          Explore the world
        </div>


        <div
          id="locationCount"
          class="location-count"
        >
          {people.length} PSGians
        </div>

      </div>


      <div class="map-hint">
        Drag to rotate · Scroll to zoom · Click a location
      </div>


      <!-- =================================================
           PERSON DETAIL
      ================================================== -->

      <div
        id="personDetails"
        class="person-details hidden"
      >

        <button
          id="closeDetails"
          class="close-details"
          type="button"
          aria-label="Close"
        >
          ×
        </button>


        <img
          id="detailImage"
          src=""
          alt=""
        />


        <div>

          <p
            id="detailCategory"
            class="detail-category"
          ></p>

          <h3 id="detailName"></h3>

          <p
            id="detailQuote"
            class="detail-quote"
          ></p>


          <div class="detail-meta">

            <span id="detailCity"></span>

            <span id="detailDepartment"></span>

            <span id="detailExpertise"></span>

          </div>

        </div>

      </div>

    </section>

  </main>


  <!-- =====================================================
       FOOTER
  ====================================================== -->

  <footer class="directory-footer">

    <span>
      © 2026 by HOP
    </span>

    <span>
      Build in India by PSGians
    </span>

  </footer>

</div>


<script define:vars={{
  people,
  cities,
  departments,
  expertise,
  categories
}}>

  /* =====================================================
     ELEMENTS
  ===================================================== */

  const container =
    document.getElementById(
      "globeContainer"
    );

  const globeLoading =
    document.getElementById(
      "globeLoading"
    );

  const loadingText =
    document.getElementById(
      "loadingText"
    );

  const mapTitle =
    document.getElementById(
      "mapTitle"
    );

  const backButton =
    document.getElementById(
      "backButton"
    );

  const locationLabel =
    document.getElementById(
      "locationLabel"
    );

  const locationCount =
    document.getElementById(
      "locationCount"
    );

  const searchInput =
    document.getElementById(
      "searchInput"
    );

  const citySelect =
    document.getElementById(
      "citySelect"
    );

  const departmentSelect =
    document.getElementById(
      "departmentSelect"
    );

  const expertiseSelect =
    document.getElementById(
      "expertiseSelect"
    );

  const categoryChips =
    document.getElementById(
      "categoryChips"
    );

  const resetButton =
    document.getElementById(
      "resetButton"
    );

  const resultCount =
    document.getElementById(
      "resultCount"
    );

  const peopleList =
    document.getElementById(
      "peopleList"
    );


  /* =====================================================
     DETAIL ELEMENTS
  ===================================================== */

  const personDetails =
    document.getElementById(
      "personDetails"
    );

  const closeDetails =
    document.getElementById(
      "closeDetails"
    );

  const detailImage =
    document.getElementById(
      "detailImage"
    );

  const detailCategory =
    document.getElementById(
      "detailCategory"
    );

  const detailName =
    document.getElementById(
      "detailName"
    );

  const detailQuote =
    document.getElementById(
      "detailQuote"
    );

  const detailCity =
    document.getElementById(
      "detailCity"
    );

  const detailDepartment =
    document.getElementById(
      "detailDepartment"
    );

  const detailExpertise =
    document.getElementById(
      "detailExpertise"
    );


  /* =====================================================
     STATE
  ===================================================== */

  let mode = "world";

  let selectedState = "";

  let selectedDistrict = "";

  let selectedCategory = "";


  let visiblePeople = [...people];

  let globe = null;


  /* =====================================================
     HELPERS
  ===================================================== */

  function normalize(value) {

    return String(value || "")
      .toLowerCase()
      .trim()
      .replace(/[^a-z0-9]/g, "");

  }


  /* =====================================================
     FILTER PEOPLE
  ===================================================== */

  function getFilteredPeople() {

    const search =
      normalize(
        searchInput.value
      );

    const city =
      normalize(
        citySelect.value
      );

    const department =
      normalize(
        departmentSelect.value
      );

    const selectedExpertise =
      normalize(
        expertiseSelect.value
      );


    return people.filter(person => {

      const text =
        normalize(
          [
            person.name,
            person.country,
            person.state,
            person.city,
            person.district,
            person.department,
            person.expertise,
            person.category,
            person.branch,
            person.address,
            person.pullQuote,
            ...(person.tags || [])
          ].join(" ")
        );


      return (

        (!search ||
          text.includes(search))

        &&

        (!city ||
          normalize(person.city) === city)

        &&

        (!department ||
          normalize(person.department) === department)

        &&

        (!selectedExpertise ||
          normalize(person.expertise) === selectedExpertise)

        &&

        (!selectedCategory ||
          normalize(person.category) ===
          normalize(selectedCategory))

        &&

        (!selectedState ||
          normalize(person.state) ===
          normalize(selectedState))

        &&

        (!selectedDistrict ||
          normalize(person.district) ===
          normalize(selectedDistrict))

      );

    });

  }


  /* =====================================================
     RENDER PEOPLE
  ===================================================== */

  function renderPeople() {

    visiblePeople =
      getFilteredPeople();


    resultCount.textContent =
      visiblePeople.length;


    peopleList.innerHTML =
      "";


    visiblePeople.forEach(person => {

      const button =
        document.createElement(
          "button"
        );


      button.className =
        "person-card";


      button.type =
        "button";


      button.innerHTML = `

        <div class="person-avatar">

          ${
            person.image

              ? `
                <img
                  src="${person.image}"
                  alt="${person.name}"
                  loading="lazy"
                />
              `

              : `
                <span>
                  ${person.name.charAt(0)}
                </span>
              `
          }

        </div>


        <div class="person-card-info">

          <strong>
            ${person.name}
          </strong>

          <span>
            ${person.city}
          </span>

          <small>
            ${person.department}
          </small>

        </div>

      `;


      button.addEventListener(
        "click",
        () => showPerson(person)
      );


      peopleList.appendChild(
        button
      );

    });


    updateGlobePoints();

  }


  /* =====================================================
     SHOW PERSON
  ===================================================== */

  function showPerson(person) {

    detailName.textContent =
      person.name;

    detailCategory.textContent =
      person.category;

    detailQuote.textContent =
      person.pullQuote;

    detailCity.textContent =
      `📍 ${person.city}`;

    detailDepartment.textContent =
      `• ${person.department}`;

    detailExpertise.textContent =
      `• ${person.expertise}`;


    if (person.image) {

      detailImage.src =
        person.image;

      detailImage.alt =
        person.name;

      detailImage.style.display =
        "block";

    } else {

      detailImage.style.display =
        "none";

    }


    personDetails.classList.remove(
      "hidden"
    );


    focusLocation(
      person.lat,
      person.lng,
      4
    );

  }


  closeDetails.addEventListener(
    "click",
    () => {

      personDetails.classList.add(
        "hidden"
      );

    }
  );


  /* =====================================================
     GLOBE
  ===================================================== */

  function buildGlobe(Globe) {

    globe =
      Globe()(container)

        .backgroundColor(
          "#eef6fb"
        )

        .globeImageUrl(
          "https://unpkg.com/three-globe/example/img/earth-blue-marble.jpg"
        )

        .bumpImageUrl(
          "https://unpkg.com/three-globe/example/img/earth-topology.png"
        )

        .showAtmosphere(
          true
        )

        .atmosphereColor(
          "#8da99a"
        )

        .atmosphereAltitude(
          0.15
        );


  /* =====================================================
     POINT SETTINGS
  ===================================================== */

  globe

    .pointLat(
      d => d.lat
    )

    .pointLng(
      d => d.lng
    )

    .pointColor(
      () => "#2478bd"
    )

    .pointRadius(
      d => {

        if (d.cluster) {
          return 0.5;
        }

        return 0.35;

      }
    )

    .pointAltitude(
      0.01
    )

    .pointLabel(
      d => {

        if (d.cluster) {

          return `
            <div class="globe-tooltip">
              <strong>${d.city}</strong>
              <br />
              ${d.count} PSGians
            </div>
          `;

        }


        return `
          <div class="globe-tooltip">
            <strong>${d.name}</strong>
            <br />
            ${d.city}
          </div>
        `;

      }
    )

    .onPointClick(
      d => {

        if (d.cluster) {

          focusLocation(
            d.lat,
            d.lng,
            4
          );


          if (mode === "world") {

            selectedState =
              "Tamil Nadu";

            mode =
              "india";

            mapTitle.textContent =
              "India";

            backButton.classList.remove(
              "hidden"
            );

            locationLabel.textContent =
              "Tamil Nadu";

          }

          return;

        }


        // Clicking a person point opens that person's details.
        showPerson(d);

      }
    )

    // Clicking ANY area of the globe finds the nearest PSGian
    // location and shows that area's people in the left panel.
    .onGlobeClick(
      coords => {

        if (!coords) return;

        // Any click on the actual globe surface selects that area.
        // The closest PSGian city is then shown in the left panel.
        showMapArea(
          Number(coords.lat),
          Number(coords.lng)
        );

      }
    );

  }


  /* =====================================================
     MAP AREA CLICK
  ===================================================== */

  function distanceKm(
    lat1,
    lng1,
    lat2,
    lng2
  ) {

    const toRad =
      value => value * Math.PI / 180;

    const earthRadius = 6371;

    const dLat =
      toRad(lat2 - lat1);

    const dLng =
      toRad(lng2 - lng1);

    const a =
      Math.sin(dLat / 2) ** 2 +
      Math.cos(toRad(lat1)) *
      Math.cos(toRad(lat2)) *
      Math.sin(dLng / 2) ** 2;

    return (
      2 *
      earthRadius *
      Math.atan2(
        Math.sqrt(a),
        Math.sqrt(1 - a)
      )
    );

  }


  function showMapArea(
    lat,
    lng
  ) {

    if (!people.length) {
      return;
    }


    // Find the nearest real location represented by our directory data.
    const nearest =
      people.reduce(
        (closest, person) => {

          const distance =
            distanceKm(
              lat,
              lng,
              Number(person.lat),
              Number(person.lng)
            );

          if (!closest || distance < closest.distance) {
            return {
              person,
              distance
            };
          }

          return closest;

        },
        null
      );


    if (!nearest || !nearest.person) {
      return;
    }


    const areaPerson =
      nearest.person;

    const areaCity =
      normalize(areaPerson.city);


    // Clear text/category filters so the complete area's data is visible.
    searchInput.value = "";
    departmentSelect.value = "";
    expertiseSelect.value = "";
    selectedCategory = "";
    selectedState = "";
    selectedDistrict = "";

    categoryChips
      .querySelectorAll(
        ".category-chip"
      )
      .forEach(
        chip => {
          chip.classList.toggle(
            "active",
            chip.dataset.category === ""
          );
        }
      );


    // Select the nearest city in the left-side city filter.
    const cityOption =
      Array.from(
        citySelect.options
      ).find(
        option =>
          normalize(option.value) === areaCity
      );

    citySelect.value =
      cityOption
        ? cityOption.value
        : areaPerson.city;


    visiblePeople =
      people.filter(
        person =>
          normalize(person.city) === areaCity
      );


    mode =
      areaPerson.state === "Tamil Nadu"
        ? "tamilNadu"
        : "india";

    selectedState =
      areaPerson.state || "";


    mapTitle.textContent =
      areaPerson.city;

    locationLabel.textContent =
      `${areaPerson.city} · ${areaPerson.state || areaPerson.country}`;

    backButton.classList.remove(
      "hidden"
    );


    // Animate the globe into a close-up of the clicked area.
    // We use the nearest directory location so the selected area and
    // the people shown in the left panel stay in sync.
    focusLocation(
      Number(areaPerson.lat),
      Number(areaPerson.lng),
      0.55,
      1800
    );


    // Re-render the left panel with ONLY this area's people.
    renderPeople();


    // Bring the newly filtered area data into view on the left.
    peopleList.scrollIntoView({
      behavior: "smooth",
      block: "nearest"
    });

  }


  /* =====================================================
     LOCATION DATA
  ===================================================== */

  function createPoints() {

    return visiblePeople.map(
      person => ({
        ...person,
        cluster: false
      })
    );

  }


  function updateGlobePoints() {

    if (!globe) return;

    globe.pointsData(
      createPoints()
    );


    locationCount.textContent =
      `${visiblePeople.length} PSGians`;

  }


  /* =====================================================
     FOCUS GLOBE
  ===================================================== */

  function focusLocation(
    lat,
    lng,
    altitude,
    duration = 1200
  ) {

    if (!globe) return;

    globe.pointOfView(
      {
        lat,
        lng,
        altitude
      },
      duration
    );

  }


  /* =====================================================
     WORLD → INDIA
  ===================================================== */

  function enterIndia() {

    mode =
      "india";

    selectedState =
      "Tamil Nadu";

    selectedDistrict =
      "";


    mapTitle.textContent =
      "India";


    locationLabel.textContent =
      "India · Tamil Nadu";


    locationCount.textContent =
      `${visiblePeople.length} PSGians`;


    backButton.classList.remove(
      "hidden"
    );


    focusLocation(
      20.5937,
      78.9629,
      1.7
    );

  }


  /* =====================================================
     INDIA → TAMIL NADU
  ===================================================== */

  function enterTamilNadu() {

    mode =
      "tamilNadu";

    selectedState =
      "Tamil Nadu";

    selectedDistrict =
      "";


    mapTitle.textContent =
      "Tamil Nadu";


    locationLabel.textContent =
      "Tamil Nadu · Explore districts";


    backButton.classList.remove(
      "hidden"
    );


    focusLocation(
      11.1271,
      78.6569,
      0.75
    );


    renderPeople();

  }


  /* =====================================================
     BACK
  ===================================================== */

  backButton.addEventListener(
    "click",
    () => {

      if (
        mode === "tamilNadu"
      ) {

        mode =
          "india";

        selectedDistrict =
          "";

        mapTitle.textContent =
          "India";

        locationLabel.textContent =
          "India · Tamil Nadu";


        focusLocation(
          20.5937,
          78.9629,
          1.7
        );


        renderPeople();

        return;

      }


      if (
        mode === "india"
      ) {

        mode =
          "world";

        selectedState =
          "";

        selectedDistrict =
          "";


        mapTitle.textContent =
          "World";


        locationLabel.textContent =
          "Explore the world";


        backButton.classList.add(
          "hidden"
        );


        focusLocation(
          20,
          0,
          2.4
        );


        renderPeople();

      }

    }
  );


  /* =====================================================
     SEARCH
  ===================================================== */

  searchInput.addEventListener(
    "input",
    renderPeople
  );


  citySelect.addEventListener(
    "change",
    renderPeople
  );


  departmentSelect.addEventListener(
    "change",
    renderPeople
  );


  expertiseSelect.addEventListener(
    "change",
    renderPeople
  );


  /* =====================================================
     CATEGORY
  ===================================================== */

  categoryChips.addEventListener(
    "click",
    event => {

      const button =
        event.target.closest(
          "[data-category]"
        );


      if (!button) {
        return;
      }


      selectedCategory =
        button.dataset.category;


      categoryChips
        .querySelectorAll(
          ".category-chip"
        )
        .forEach(
          chip => {

            chip.classList.toggle(
              "active",
              chip === button
            );

          }
        );


      renderPeople();

    }
  );


  /* =====================================================
     RESET
  ===================================================== */

  resetButton.addEventListener(
    "click",
    () => {

      searchInput.value =
        "";

      citySelect.value =
        "";

      departmentSelect.value =
        "";

      expertiseSelect.value =
        "";


      selectedCategory =
        "";

      selectedState =
        "";

      selectedDistrict =
        "";


      categoryChips
        .querySelectorAll(
          ".category-chip"
        )
        .forEach(
          (chip, index) => {

            chip.classList.toggle(
              "active",
              index === 0
            );

          }
        );


      mode =
        "world";


      mapTitle.textContent =
        "World";


      locationLabel.textContent =
        "Explore the world";


      backButton.classList.add(
        "hidden"
      );


      personDetails.classList.add(
        "hidden"
      );


      focusLocation(
        20,
        0,
        2.4
      );


      renderPeople();

    }
  );


  /* =====================================================
     MAP INTERACTION
  ===================================================== */

  container.addEventListener(
    "dblclick",
    event => {

      event.preventDefault();

    }
  );


  /* =====================================================
     INITIALIZE GLOBE AFTER CDN LOAD
  ===================================================== */

  function startGlobe() {

    globeLoading.classList.remove("hidden");
    loadingText.textContent = "Loading globe...";

    const existing = window.Globe;

    if (existing) {
      buildGlobe(existing);
      finishGlobeLoading();
      return;
    }

    const script = document.createElement("script");
    script.src = "https://unpkg.com/globe.gl@2.43.0/dist/globe.gl.min.js";
    script.async = true;

    script.onload = () => {
      if (!window.Globe) {
        showGlobeError();
        return;
      }

      buildGlobe(window.Globe);
      finishGlobeLoading();
    };

    script.onerror = () => {
      // Fallback CDN
      const fallback = document.createElement("script");
      fallback.src = "https://cdn.jsdelivr.net/npm/globe.gl@2.43.0/dist/globe.gl.min.js";
      fallback.async = true;

      fallback.onload = () => {
        if (!window.Globe) {
          showGlobeError();
          return;
        }

        buildGlobe(window.Globe);
        finishGlobeLoading();
      };

      fallback.onerror = showGlobeError;
      document.head.appendChild(fallback);
    };

    document.head.appendChild(script);
  }


  function finishGlobeLoading() {

    globe.pointOfView(
      {
        lat: 20,
        lng: 0,
        altitude: 2.4
      },
      0
    );

    renderPeople();

    const resizeGlobe = () => {
      if (!globe) return;

      globe.width(container.clientWidth);
      globe.height(container.clientHeight);
    };

    window.addEventListener("resize", resizeGlobe);

    requestAnimationFrame(() => {
      resizeGlobe();
    });

    setTimeout(() => {
      globeLoading.classList.add("hidden");
    }, 500);
  }


  function showGlobeError() {

    loadingText.textContent = "Map could not load";

    console.error("Globe.gl could not be loaded from the CDN.");
  }


  startGlobe();

</script>


<style>
  @import url("https://fonts.cdnfonts.com/css/fedora");
  @import url("https://fonts.cdnfonts.com/css/futura-std-4");

  /* Requested typography: Fedora for headings, Futura for subheadings/body UI. */

  /* =====================================================
     HOP LIGHT UI — VISUAL DESIGN ONLY
     Content / data / JS behavior remain unchanged
  ===================================================== */

  :global(html) {
    background: #ffffff;
    color-scheme: light;
  }

  :global(body) {
    margin: 0;
    background: #ffffff;
    color: #241c18;
    font-family: "Futura Std", "Futura", "Century Gothic", Arial, Helvetica, sans-serif;
    font-size: 15px;
  }

  * {
    box-sizing: border-box;
  }

  button,
  input,
  select {
    font: inherit;
  }

  /* =====================================================
     PAGE
  ===================================================== */

  .directory-page {
    min-height: 100vh;
    background: #ffffff;
    color: #241c18;
  }

  /* =====================================================
     HEADER
  ===================================================== */

  .directory-header {
    padding: 42px 5vw 30px;
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 30px;
    border-bottom: 1px solid #eee2da;
    background: #ffffff;
  }

  .eyebrow,
  .map-eyebrow {
    margin: 0 0 10px;
    color: #8b7569;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.18em;
  }

  /* Fedora-style display typography for all headings */
  .directory-header h1,
  .directory-page h2,
  .directory-page h3,
  .directory-page h4,
  .directory-page h5,
  .directory-page h6 {
    font-family: "FEDORA", "Fedora", "Federo", Georgia, serif;
  }

  .directory-header h1 {
    margin: 0;
    max-width: 1000px;
    color: #241c18;
    font-size: clamp(42px, 4.6vw, 64px);
    line-height: 1.02;
    font-weight: 800;
    letter-spacing: -0.055em;
  }

  .subtitle {
    font-family: "Futura Std", "Futura", "Century Gothic", Arial, Helvetica, sans-serif;
    margin: 14px 0 0;
    max-width: 650px;
    color: #8b7569;
    font-size: 15px;
    line-height: 1.7;
  }

  .header-count {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    flex: 0 0 auto;
    padding-bottom: 3px;
  }

  .header-count strong {
    color: #2478bd;
    font-size: 36px;
    line-height: 1;
    font-weight: 800;
  }

  .header-count span {
    margin-top: 6px;
    color: #8b7569;
    font-size: 11px;
  }

  /* =====================================================
     MAIN LAYOUT
  ===================================================== */

  .directory-layout {
    display: grid;
    grid-template-columns: 360px minmax(0, 1fr);
    align-items: stretch;
    min-height: calc(100vh - 190px);
  }

  /* =====================================================
     LEFT FILTER PANEL
     Right side is reserved for the interactive map.
  ===================================================== */

  .directory-sidebar {
    width: 100%;
    min-width: 0;
    padding: 30px 24px 28px;
    background: #ffffff;
    border-right: 1px solid #eee2da;
    overflow: hidden;
  }

  .search-box {
    width: 100%;
    height: 42px;
    display: flex;
    align-items: center;
    gap: 9px;
    padding: 0 14px;
    border: 1px solid #ead9cf;
    border-radius: 999px;
    background: #ffffff;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .search-box:focus-within {
    border-color: #2b7fc2;
    box-shadow: 0 0 0 3px rgba(43, 127, 194, 0.08);
  }

  .search-box span {
    color: #8d776b;
    font-size: 19px;
    line-height: 1;
  }

  .search-box input {
    width: 100%;
    border: 0;
    outline: 0;
    background: transparent;
    color: #241c18;
    font-size: 13px;
  }

  .search-box input::placeholder {
    color: #9d8b81;
  }

  .filter-group {
    margin-top: 12px;
  }

  .filter-group label,
  .section-title {
    display: block;
    margin: 0 0 7px;
    color: #8b7569;
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
  }

  .filter-group select {
    width: 100%;
    height: 40px;
    padding: 0 38px 0 14px;
    border: 1px solid #ead9cf;
    border-radius: 999px;
    outline: 0;
    background: #ffffff;
    color: #4f4038;
    font-size: 12px;
    cursor: pointer;
    appearance: auto;
  }

  .filter-group select:focus {
    border-color: #2b7fc2;
    box-shadow: 0 0 0 3px rgba(43, 127, 194, 0.08);
  }

  /* Stack all filters vertically in the left panel */
  .directory-sidebar {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .directory-sidebar .search-box,
  .directory-sidebar .filter-group,
  .directory-sidebar .category-section,
  .directory-sidebar .reset-button,
  .directory-sidebar .result-count,
  .directory-sidebar .people-list {
    grid-column: auto;
    grid-row: auto;
  }

  .directory-sidebar .search-box {
    order: 1;
    margin-bottom: 16px;
  }

  .directory-sidebar .filter-group {
    order: 2;
    margin-top: 12px;
  }

  .directory-sidebar .filter-group:nth-of-type(1),
  .directory-sidebar .filter-group:nth-of-type(2),
  .directory-sidebar .filter-group:nth-of-type(3) {
    width: 100%;
  }

  /* =====================================================
     CATEGORIES
  ===================================================== */

  .category-section {
    order: 3;
    margin-top: 18px;
  }

  .category-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
  }

  .category-chip {
    padding: 7px 13px;
    border: 1px solid #ead9cf;
    border-radius: 999px;
    background: #ffffff;
    color: #765f53;
    cursor: pointer;
    font-size: 11px;
    transition: all 0.18s ease;
  }

  .category-chip:hover {
    border-color: #b9d5ea;
    color: #2478bd;
  }

  .category-chip.active {
    background: #2b7fc2;
    border-color: #2b7fc2;
    color: #ffffff;
  }

  /* =====================================================
     RESET / RESULT COUNT
  ===================================================== */

  .reset-button {
    order: 4;
    width: fit-content;
    min-width: 130px;
    height: 38px;
    margin-top: 15px;
    padding: 0 16px;
    border: 1px solid #ead9cf;
    border-radius: 999px;
    background: #fffaf7;
    color: #765f53;
    cursor: pointer;
    font-size: 11px;
    transition: all 0.18s ease;
  }

  .reset-button:hover {
    border-color: #2b7fc2;
    color: #2478bd;
    background: #ffffff;
  }

  .result-count {
    order: 5;
    margin-top: 20px;
    padding: 9px 0 8px;
    border-bottom: 1px solid #eee2da;
    color: #8b7569;
    font-size: 11px;
    text-align: right;
  }

  .result-count strong {
    color: #2478bd;
    font-weight: 800;
  }

  /* =====================================================
     PEOPLE LIST
     Screenshot-inspired horizontal profile row
  ===================================================== */

  .people-list {
    order: 6;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    align-items: flex-start;
    gap: 18px 10px;
    width: 100%;
    max-height: 430px;
    margin-top: 18px;
    padding: 0 0 10px;
    overflow-x: hidden;
    overflow-y: auto;
    scrollbar-width: thin;
    scrollbar-color: #ddcfc6 transparent;
  }

  .people-list::-webkit-scrollbar {
    width: 5px;
  }

  .people-list::-webkit-scrollbar-track {
    background: transparent;
  }

  .people-list::-webkit-scrollbar-thumb {
    background: #ddcfc6;
    border-radius: 999px;
  }

  :global(.person-card) {
    appearance: none;
    -webkit-appearance: none;
    font: inherit;
    width: 88px;
    min-width: 88px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    padding: 0;
    border: 0;
    background: transparent;
    color: #765f53;
    text-align: center;
    cursor: pointer;
  }

  :global(.person-card:hover) {
    background: transparent;
  }

  :global(.person-avatar) {
    width: 78px;
    height: 78px;
    flex: 0 0 78px;
    overflow: hidden;
    border-radius: 50%;
    background: #f1ebe6;
    border: 1px solid #e4d8d0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #2b7fc2;
    font-weight: 800;
    box-shadow: 0 3px 12px rgba(47, 34, 26, 0.06);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  :global(.person-card:hover .person-avatar) {
    transform: translateY(-3px);
    box-shadow: 0 8px 18px rgba(47, 34, 26, 0.10);
  }

  :global(.person-avatar img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  :global(.person-card-info) {
    width: 100%;
    min-width: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  :global(.person-card-info strong) {
    width: 100%;
    overflow: hidden;
    color: #6f5b51;
    font-size: 10px;
    font-weight: 500;
    line-height: 1.25;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  :global(.person-card-info span),
  :global(.person-card-info small) {
    width: 100%;
    overflow: hidden;
    margin-top: 2px;
    color: #a28f84;
    font-size: 9px;
    line-height: 1.2;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  /* =====================================================
     MAP SECTION
  ===================================================== */

  .map-section {
    position: relative;
    min-width: 0;
    margin-top: 0;
    padding: 30px 32px 50px;
    border-left: 0;
    border-top: 0;
    background: #fffaf7;
  }

  .map-topbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
  }

  .map-topbar h2 {
    margin: 0;
    color: #241c18;
    font-size: 27px;
    line-height: 1.1;
    font-weight: 700;
    letter-spacing: -0.03em;
  }

  .back-button {
    padding: 8px 14px;
    border: 1px solid #ead9cf;
    border-radius: 999px;
    background: #ffffff;
    color: #765f53;
    cursor: pointer;
    font-size: 11px;
  }

  .back-button:hover {
    border-color: #2b7fc2;
    color: #2478bd;
  }

  .hidden {
    display: none !important;
  }

  /* =====================================================
     GLOBE CONTAINER
  ===================================================== */

  .globe-container {
    position: relative;
    width: 100%;
    height: min(68vh, 680px);
    min-height: 480px;
    margin-top: 14px;
    overflow: hidden;
    border: 1px solid #dbe7ef;
    border-radius: 24px;
    background:
      radial-gradient(circle at 50% 42%, rgba(255,255,255,.96) 0%, rgba(244,249,252,.98) 42%, rgba(232,242,248,1) 100%);
    box-shadow: 0 14px 38px rgba(36, 63, 79, 0.10);
  }

  .globe-container::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      linear-gradient(90deg, rgba(255,255,255,.32), transparent 22%, transparent 78%, rgba(255,255,255,.32)),
      linear-gradient(180deg, rgba(255,255,255,.28), transparent 25%, transparent 75%, rgba(210,226,236,.18));
    z-index: 1;
  }

  .globe-container canvas {
    width: 100% !important;
    height: 100% !important;
    display: block;
  }

  /* =====================================================
     GLOBE LOADING
  ===================================================== */

  .globe-loading {
    position: absolute;
    z-index: 50;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 11px 17px;
    border: 1px solid #ead9cf;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.96);
    color: #765f53;
    font-size: 12px;
    white-space: nowrap;
    box-shadow: 0 10px 35px rgba(47, 34, 26, 0.10);
    pointer-events: none;
  }

  .loading-spinner {
    width: 14px;
    height: 14px;
    flex: 0 0 14px;
    border: 2px solid #ead9cf;
    border-top-color: #2b7fc2;
    border-radius: 50%;
    animation: globe-loading-spin 0.8s linear infinite;
  }

  @keyframes globe-loading-spin {
    to {
      transform: rotate(360deg);
    }
  }

  /* =====================================================
     CROSSHAIR
  ===================================================== */

  .globe-crosshair {
    position: absolute;
    z-index: 4;
    left: 50%;
    top: 50%;
    width: 42px;
    height: 42px;
    transform: translate(-50%, -50%);
    pointer-events: none;
    opacity: 0.12;
  }

  .globe-crosshair::before,
  .globe-crosshair::after {
    content: "";
    position: absolute;
    background: #2b7fc2;
  }

  .globe-crosshair::before {
    left: 20px;
    top: 0;
    width: 1px;
    height: 42px;
  }

  .globe-crosshair::after {
    top: 20px;
    left: 0;
    width: 42px;
    height: 1px;
  }

  /* =====================================================
     LOCATION LABELS
  ===================================================== */

  .location-label {
    position: absolute;
    z-index: 5;
    left: 22px;
    bottom: 22px;
    padding: 9px 13px;
    border-radius: 999px;
    border: 1px solid #ead9cf;
    background: rgba(255, 255, 255, 0.92);
    color: #765f53;
    font-size: 12px;
    pointer-events: none;
  }

  .location-count {
    position: absolute;
    z-index: 5;
    right: 22px;
    bottom: 22px;
    padding: 9px 13px;
    border-radius: 999px;
    border: 1px solid #d6e5ef;
    background: rgba(255, 255, 255, 0.92);
    color: #2478bd;
    font-size: 12px;
    pointer-events: none;
  }

  .map-hint {
    margin-top: 10px;
    color: #9b887d;
    font-size: 11px;
  }

  /* =====================================================
     PERSON DETAIL
  ===================================================== */

  .person-details {
    position: relative;
    display: flex;
    gap: 20px;
    margin-top: 20px;
    padding: 18px;
    border: 1px solid #ead9cf;
    border-radius: 16px;
    background: #ffffff;
    box-shadow: 0 8px 25px rgba(47, 34, 26, 0.06);
  }

  .person-details > img {
    width: 100px;
    height: 125px;
    object-fit: cover;
    border-radius: 10px;
    border: 1px solid #ead9cf;
    background: #f1ebe6;
  }

  .detail-category {
    margin: 0 0 5px;
    color: #2b7fc2;
    font-size: 9px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
  }

  .person-details h3 {
    margin: 0;
    color: #241c18;
    font-size: 22px;
    font-weight: 700;
  }

  .detail-quote {
    max-width: 600px;
    margin: 10px 0;
    color: #7c685d;
    font-size: 13px;
    line-height: 1.6;
  }

  .detail-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    color: #8e7b70;
    font-size: 10px;
  }

  .close-details {
    position: absolute;
    right: 10px;
    top: 7px;
    width: 30px;
    height: 30px;
    border: 0;
    border-radius: 50%;
    background: #fffaf7;
    color: #806c61;
    font-size: 22px;
    line-height: 1;
    cursor: pointer;
  }

  .close-details:hover {
    color: #2478bd;
    background: #eef6fb;
  }

  /* =====================================================
     TOOLTIP
  ===================================================== */

  :global(.globe-tooltip) {
    padding: 8px 10px;
    border: 1px solid #ead9cf;
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.97);
    color: #241c18;
    font-family: "Futura Std", "Futura", "Century Gothic", Arial, Helvetica, sans-serif;
    font-size: 11px;
    box-shadow: 0 8px 22px rgba(47, 34, 26, 0.12);
  }

  :global(.globe-tooltip strong) {
    color: #2478bd;
  }

  /* =====================================================
     FOOTER
  ===================================================== */

  .directory-footer {
    display: flex;
    justify-content: space-between;
    gap: 20px;
    padding: 20px 5vw;
    border-top: 1px solid #eee2da;
    background: #ffffff;
    color: #9a877c;
    font-size: 10px;
  }

  /* =====================================================
     MOBILE
  ===================================================== */

  @media (max-width: 900px) {
    .directory-header {
      padding: 34px 24px 26px;
    }

    .directory-layout {
      grid-template-columns: 1fr;
    }

    .directory-sidebar {
      padding: 26px 24px 16px;
      border-right: 0;
      border-bottom: 1px solid #eee2da;
    }

    .directory-sidebar .search-box,
    .directory-sidebar .filter-group,
    .directory-sidebar .category-section,
    .directory-sidebar .reset-button,
    .directory-sidebar .result-count,
    .directory-sidebar .people-list {
      grid-column: auto;
      grid-row: auto;
    }

    .people-list {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 18px 10px;
      max-height: 380px;
    }

    .map-section {
      padding: 26px 24px 40px;
    }

    .globe-container {
      min-height: 430px;
      height: 55vh;
    }
  }

  @media (max-width: 600px) {
    .directory-header {
      padding: 28px 18px 24px;
      align-items: flex-start;
      flex-direction: column;
    }

    /* Fedora-style display typography for all headings */
  .directory-header h1,
  .directory-page h2,
  .directory-page h3,
  .directory-page h4,
  .directory-page h5,
  .directory-page h6 {
    font-family: "FEDORA", "Fedora", "Federo", Georgia, serif;
  }

  .directory-header h1 {
      font-size: 34px;
    }

    .subtitle {
    font-family: "Futura Std", "Futura", "Century Gothic", Arial, Helvetica, sans-serif;
      font-size: 13px;
    }

    .header-count {
      align-items: flex-start;
    }

    .directory-sidebar {
      padding: 22px 18px 14px;
    }

    .result-count {
      text-align: left;
    }

    .people-list {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 20px 8px;
    }

    .map-section {
      padding: 22px 12px 30px;
    }

    .map-topbar h2 {
      font-size: 23px;
    }

    .globe-container {
      min-height: 380px;
      height: 55vh;
      border-radius: 14px;
    }

    .person-details {
      flex-direction: column;
    }

    .person-details > img {
      width: 100%;
      max-width: 140px;
      height: 150px;
    }

    .directory-footer {
      flex-direction: column;
      gap: 7px;
      padding: 18px;
    }
  }
</style>