---
import type { CollectionEntry } from 'astro:content';
import { resolvePhotoUrl } from '../lib/photoUrl';
import { defaultPhotoAlt } from '../lib/photoAlt';

interface Props {
  story: CollectionEntry<'stories'>;
}

const { story } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<a class="story-card reveal" href={`${base}psgians/${story.id}/`}>
  <div class="story-card-media">
    <img src={resolvePhotoUrl(base, story.data.photo)} alt={defaultPhotoAlt(story.data.name)} loading="lazy" />
  </div>
  <p class="meta">{story.data.location}</p>
  <h2>{story.data.name}</h2>
  <p>{story.data.pullQuote}</p>
  <span class="read-more-pill">Read story &rarr;</span>
</a>
